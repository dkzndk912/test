week02
- linux command : ls, ls -al, pwd, cd .git, cd .. echo "bblblalbl" >> README.md cat README.md
- git command
  git init
  git status
  git add week01.txt
  git commit -m "v0"
  git log
  git config --global user.email "you@example.com"
  git config --global user.name "your name"
  git checkout hashcode
  git checkout -
  git branch -M main
  git push -u origin main
  git remote add origin https://github.com/dkzndk912/test.git
  git clone https://github.com/dkzndk912/test.git
  git clone https://github.com/dkzndk912/test.git .
  